/**
 * サンプル設定ファイル
 * 
 * 現在は Cloudflare KV に設定が保存されるため、このファイルは参考用です。
 * KV に設定が存在しない場合のみ、この設定がデフォルトとして使用されます。
 * 
 * 実際の設定は /settings ページからブラウザで編集できます。
 */
export const excludeConfig = {
	// タイトルに対する除外パターン
	title: [
		// 例: '^PR:', '【広告】'
	],
	// リンクURLに対する除外パターン
	link: [
		// 例: 'ad\\.example\\.com'
	],
};
